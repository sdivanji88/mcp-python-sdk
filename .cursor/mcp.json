{
    "mcpServers": {
      "image-mcp": {
        "url": "http://localhost:9090/sse",
        "env": {
          "MCP_SERVER_PORT": "9090",
          "MCP_SERVER_TRANSPORT": "sse",
          "PYTHONPATH": "${workspaceFolder}/examples/servers/image-mcp"
        }
      }
    }
  }